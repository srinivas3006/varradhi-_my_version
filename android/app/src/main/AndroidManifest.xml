<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
        <!-- Saving downloaded posters to the gallery. Write-only and capped:
         gal uses this when SDK_INT <= 29 and writes through MediaStore above
         that. maxSdkVersion must match gal's own bound (29), not 28, or the
         save fails on Android 10. No photo/video READ permission is needed:
         downloading writes a file we generated, it never reads the library. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

<application
        android:label="VARADHI"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <meta-data
                android:name="flutter_deeplinking_enabled"
                android:value="false" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep link intent filter for custom scheme varadhi:// -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="varadhi" />
            </intent-filter>

            <!-- Android App Links for the public site.
                 autoVerify only takes effect once
                 https://vaaradhinews.com/.well-known/assetlinks.json is
                 reachable; the file is in web/.well-known/ ready to publish.
                 Until it is live these links open the browser as before, and
                 on Android 11 and below a chooser may appear — so publish
                 assetlinks.json before shipping this build. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="vaaradhinews.com" />
                <data android:scheme="https" android:host="www.vaaradhinews.com" />
                <data android:pathPrefix="/article/" />
                <data android:pathPrefix="/poster/" />
                <data android:pathPrefix="/poll/" />
                <data android:pathPrefix="/ugc/" />
                <data android:pathPrefix="/video/" />
                <data android:pathPrefix="/short/" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
            <!-- Background/terminated FCM messages are rendered by the OS, not
             by Dart, so the channel has to be named here too. Without it
             Android drops them into a low-importance fallback channel: they
             arrive silently, with no heads-up banner. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="vaaradhi_news" />
</application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Android 11+ package visibility. Without these, canLaunchUrl()
             cannot see a handler and returns false, so every ad click-through
             silently did nothing even with a destination_url present — the
             guard failed, not the launch. Ten widgets share that guard. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <!-- wa.me links open the WhatsApp app when it is installed. -->
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
    </queries>
</manifest>
