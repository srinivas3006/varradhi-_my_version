# Step 9 Test Matrix — Production Reliability & Failure Boundary Validation

| Category | Test Case / Boundary Scenario | Component / Subsystem | Injected Fault / Trigger | Expected Behavior | Actual Verified Result | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **Storage Resilience** | KeyStore corruption on boot | `AppState` / `FlutterSecureStorage` | `PlatformException` during `readAll()` | Catches exception, wipes corrupted KeyStore entries, initializes clean guest state | Logged warning, wiped keys, defaulted to guest session | **PASS** |
| **Storage Resilience** | SharedPreferences corrupted types | `AppState` | Corrupted non-string/corrupted format in local prefs | Fallback to safe defaults without crashing UI | Handled gracefully, guest session established | **PASS** |
| **Network Failure** | 502 Bad Gateway with HTML body | `FeedRepository` | Nginx/Cloudflare 502 returning `<html>...</html>` string | Type guard catches non-Map, converts to `NetworkException` with Telugu translation | Prevents `TypeError: type 'String' is not a subtype of 'Map'`, returns error state | **PASS** |
| **Network Failure** | 500 Internal Server Error | `DioClient` / `FeedRepository` | HTTP 500 response from backend | Translated to `ServerException` with user-friendly Telugu message | Handled cleanly, retry banner displayed | **PASS** |
| **Network Failure** | 401 Unauthorized for Guest | `DioClient` / `AppState` | HTTP 401 on guest public endpoint | Does NOT trigger auth logout / redirect for unauthenticated users | Guest state preserved, no spurious logout flash | **PASS** |
| **Network Failure** | Full offline disconnect | `FeedRepository` / `ApiClient` | `SocketException` / offline connection refused | Serves persistent cached feed instantly, shows offline status | Seamless offline cache read, zero unhandled errors | **PASS** |
| **Network Failure** | Slow network connection timeout | `DioClient` | Connect timeout > 15s, receive timeout > 15s | Throws `TimeoutException`, prompts user with retry button | Gracefully caught, UI displays retry action | **PASS** |
| **Payload Anomaly** | Missing or malformed list items | `FeedRepository` | Feed response containing null or partial article dicts | Skips invalid items via `whereType<Map>()` and null guards | Valid items parsed successfully, invalid items discarded safely | **PASS** |
| **Payload Anomaly** | Empty or non-standard JSON feed | `FeedRepository` | Empty JSON object `{}` or missing `data` array | Returns empty list cleanly without throwing null pointer exceptions | Handled cleanly, empty feed state returned | **PASS** |
| **UGC Failure** | Upload cancellation mid-stream | `UgcRepository` / `UgcController` | `CancelToken.cancel()` called while multipart upload in flight | Propagates `DioException(type: cancel)`, sets status to cancelled | Upload halted immediately, resources released, UI reset | **PASS** |
| **UGC Failure** | Server 500 during media upload | `UgcRepository` / `UgcController` | 500 Internal Server Error on `/api/v1/ugc/upload-media/` | Propagates `DioException`, preserves `submissionId` and draft | Submission ID preserved, draft saved, retry button available | **PASS** |
| **UGC Failure** | Corrupted draft JSON file | `UgcDraftRepository` | Malformed JSON in `.varadhi_ugc_draft.json` | Catches `FormatException`, wipes corrupt file, returns null | Catches error safely, draft wiped, fresh submission form shown | **PASS** |
| **Navigation** | Unmounted context navigation | `AppNavigator` | Route push attempted after parent widget unmounted | `if (!context.mounted) return null;` guard | Returns null immediately, zero framework crash / assertion error | **PASS** |
| **Navigation** | Android back button double-tap | `HomeScreen` / `AppNavigator` | Rapid back gestures in home root tab | `PopScope` handles root tab switching before exit prompt | Deterministic exit dialog, no navigation stack corruption | **PASS** |
| **Navigation** | Predictive back gesture | Android 14/15 system gesture | System edge-swipe back gesture | Intercepted cleanly by `PopScope(canPop: false)` | Respects internal tab stack, returns to Feed before exit | **PASS** |
| **Deep-Linking** | Custom URI scheme `varadhi://article/` | `NotificationDeepLinkResolver` | Incoming deep link for published article | Resolves target to `NotificationTargetType.article` | Route pushed safely to `ArticleDetailScreen` | **PASS** |
| **Deep-Linking** | Malformed / empty deep link | `NotificationDeepLinkResolver` | Incoming deep link `varadhi://` or invalid syntax | Fallback to `NotificationTargetType.unknown` | Safely ignored or directed to Home tab, no crash | **PASS** |
| **Notifications** | Android 13+ permission denial | `NotificationService` | User denies POST_NOTIFICATIONS runtime permission | Persists denial state in prefs, does not re-prompt on cold launch | Avoids spamming permission dialog on every boot | **PASS** |
| **Notifications** | Terminated app launch via push | `NotificationService` | Push tapped while app killed | `getInitialMessage()` decoded and queued for navigation | Post-frame callback navigates after widget tree mounts | **PASS** |
| **Ad Engine** | Unbounded offline queue growth | `AdEventQueue` | User scrolls 100+ items while completely offline | Queue capped at `maxQueueSize = 50`, oldest evicted | Bounded memory consumption, FIFO drop of stale events | **PASS** |
| **Ad Engine** | Impression deduplication | `AdManager` | Repeated scroll exposures of identical ad slot | Deduplicated per `adId + placementZone + contextKey` | Single emission per exposure key, no event flooding | **PASS** |
| **Resource Leaks** | TextEditingController disposal | `CommentsScreen` / `ProfileTab` | Modal dialog dismissed via back or outside tap | Explicit `.whenComplete(() => controller.dispose())` | Controller disposed cleanly, zero memory or listener leak | **PASS** |
| **Media Engine** | Invalid / empty video URL | `VideoPlaybackController` | Media item with blank network video URL | Catches initialization error, transitions to `PlaybackStatus.error` | Error message displayed, player handles failure without crash | **PASS** |
